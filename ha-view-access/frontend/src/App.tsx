import * as React from 'react';
import UserViewGrid from './pages/UserViewGrid';

export default function App() {
    return (
        <React.Fragment>
            <UserViewGrid />
        </React.Fragment>
    );
}
